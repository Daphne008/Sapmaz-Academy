const translations = {
"register_page_title": {
tr: "Online Kayıt | Sapmaz Academy",
en: "Online Registration | Sapmaz Academy"
},
"nav_about": {
tr: "Hakkımızda",
en: "About Us"
},
"nav_staff": {
tr: "Kadromuz",
en: "Our Staff"
},
"nav_courses": {
tr: "Eğitimler",
en: "Courses"
},
"nav_resources": {
tr: "Kaynaklar",
en: "Resources"
},
"nav_news": {
tr: "Haberler",
en: "News"
},
"nav_contact": {
tr: "İletişim",
en: "Contact"
},
"bc_home": {
tr: "Ana Sayfa",
en: "Home"
},
"reg_page_breadcrumb": {
tr: "Online Kayıt",
en: "Online Registration"
},
"reg_page_title": {
tr: "Online Kurs Kayıt Paneli",
en: "Online Course Registration Panel"
},
"reg_page_desc": {
tr: "Bilgilerinizi eksiksiz doldurarak kurs başvurunuzu tamamlayın.",
en: "Complete your course application by filling out your details fully."
},
"reg_select_session_label": {
tr: "Seçilen Eğitim Dönemi",
en: "Selected Training Session"
},
"reg_select_session": {
tr: "Kayıt Olmak İstediğiniz Dönem",
en: "Session You Wish to Register For"
},
"reg_lbl_name": {
tr: "Adınız Soyadınız *",
en: "Your Full Name *"
},
"reg_lbl_id": {
tr: "T.C. Kimlik / Pasaport No *",
en: "ID / Passport Number *"
},
"reg_lbl_mail": {
tr: "E-Posta Adresiniz *",
en: "Your Email Address *"
},
"reg_lbl_phone": {
tr: "Telefon Numaranız *",
en: "Your Phone Number *"
},
"reg_lbl_document": {
tr: "Mezuniyet Belgesi veya Öğrenci Belgesi *",
en: "Graduation or Student Certificate *"
},
"reg_lbl_headshot": {
tr: "Vesikalık Fotoğraf *",
en: "Passport-sized Headshot Photo *"
},
"reg_lbl_address": {
tr: "Açık Adresiniz *",
en: "Open Address *"
},
"reg_lbl_id_front": {
tr: "Kimlik Ön Yüzü Fotoğrafı *",
en: "Front ID Picture *"
},
"reg_lbl_id_back": {
tr: "Kimlik Arka Yüzü Fotoğrafı *",
en: "Back ID Picture *"
},
"reg_lbl_capacity": {
tr: "Başvuru Kapasitesi *",
en: "Application Capacity *"
},
"reg_lbl_capacity_other": {
tr: "Lütfen Belirtiniz *",
en: "Please Specify *"
},
"opt_select_capacity": {
tr: "Kapasite Seçiniz...",
en: "Select Capacity..."
},
"opt_capacity_individual": {
tr: "Bireysel Öğrenci",
en: "Individual Student"
},
"opt_capacity_company": {
tr: "Kurumsal / Şirket",
en: "Corporate / Company"
},
"opt_capacity_other": {
tr: "Diğer",
en: "Other"
},
"reg_pay_terms_title": {
tr: "Ödeme Koşulları & Taksit Seçenekleri",
en: "Payment Methods & Installment Options"
},
"reg_pay_terms_desc": {
tr: "Eğitim ödemelerinizi tek çekim veya anlaşmalı kredi kartlarına (Bonus, Maximum, World, Axess, CardFinans, Paraf) 3, 6 veya 9 aya varan taksit seçenekleriyle gerçekleştirebilirsiniz. Nakit veya Havale/EFT ile yapılan ödemelerde %5 indirim uygulanmaktadır.",
en: "You can make your course payments in a single transaction or with installment plans up to 3, 6, or 9 months on contracted credit cards (Bonus, Maximum, World, Axess, CardFinans, Paraf). A 5% discount is applied for cash or bank wire (EFT/Transfer) payments."
},
"reg_submit": {
tr: "Kaydı Tamamla",
en: "Complete Registration"
},
"reg_select_file": {
tr: "Dosya Seç (PDF, PNG, JPG)",
en: "Select File (PDF, PNG, JPG)"
},
"reg_select_photo": {
tr: "Fotoğraf Seç (PNG, JPG)",
en: "Select Photo (PNG, JPG)"
},
"opt_select": {
tr: "Seçiniz...",
en: "Select..."
},
"reg_success_title": {
tr: "Başvurunuz Başarıyla Alındı!",
en: "Application Received Successfully!"
},
"reg_success_desc": {
tr: "Ön kayıt başvuru formunuz başarıyla gönderilmiş ve cihazınıza kaydedilmiştir. Dönem ücreti ödemesini ibraz edilen IBAN adresine havale/EFT yaptıktan sonra kaydınız kesinleşecektir.",
en: "Your pre-registration application has been successfully submitted and saved. Your registration will be finalized after transferring the term fee to the designated IBAN."
},
"reg_bank_info": {
tr: "Banka Ödeme Detayları",
en: "Bank Payment Details"
},
"reg_bank": {
tr: "Banka:",
en: "Bank:"
},
"reg_iban": {
tr: "IBAN:",
en: "IBAN:"
},
"reg_holder": {
tr: "Alıcı:",
en: "Beneficiary:"
},
"success_back_btn": {
tr: "Ana Sayfaya Dön",
en: "Back to Homepage"
},
"footer_title_about": {
tr: "Sapmaz Havacılık",
en: "Sapmaz Aviation"
},
"footer_desc": {
tr: "Havacılık sektörüne yön veren Sapmaz Grubu, pilotluk ve drone teknolojilerindeki 20 yılı asker tecrübesiyle profesyoneller yetiştirir.",
en: "Leading the aviation industry, Sapmaz Group trains professionals with over 20 years of experience in flight training & drone technology."
},
"footer_title_links": {
tr: "Kurumsal Bilgiler",
en: "Information Hub"
},
"footer_link_accreditations": {
tr: "Sertifikalar ve Yetkilerimiz",
en: "Accreditations & Certificates"
},
"footer_link_careers": {
tr: "Havacılık Kariyer Rehberi",
en: "Aviation Careers Guide"
},
"footer_link_privacy": {
tr: "Gizlilik ve KVKK Politikası",
en: "Privacy & GDPR Data Policy"
},
"footer_link_terms": {
tr: "Kullanım Şartları",
en: "Terms & Conditions"
},
"footer_title_contact": {
tr: "İletişim Bilgileri",
en: "Contact Information"
},
"contact_address_label": {
tr: "Merkez Ofis",
en: "Headquarters"
},
"contact_address": {
tr: "Sapmaz Plaza, Kat: 4, Havacılık Cd. No:12, Kadıköy / İstanbul",
en: "Sapmaz Plaza, Floor: 4, Havacilik St. No:12, Kadikoy / Istanbul"
},
"contact_email_label": {
tr: "E-Posta",
en: "Email Address"
},
"contact_phone_label": {
tr: "Müşteri İlişkileri",
en: "Customer Relations"
},
"footer_rights": {
tr: "Tüm Hakları Saklıdır.",
en: "All Rights Reserved."
},
"footer_design": {
tr: "Havacılık Eğitim Sistemleri Tasarımı",
en: "Aviation Training Systems Design"
}
};
document.addEventListener("DOMContentLoaded", () => {
setupMobileMenu();
setupTheme();
setupLanguage();
setupRegistrationSessions();
setupFilePreviews();
setupSubmitHandler();
});
function setupMobileMenu() {
const hamburger = document.getElementById("hamburger");
const navMenu = document.getElementById("navMenu");
const navLinks = document.querySelectorAll(".nav-link");
if (hamburger && navMenu) {
hamburger.addEventListener("click", () => {
hamburger.classList.toggle("open");
navMenu.classList.toggle("open");
});
navLinks.forEach(link => {
link.addEventListener("click", () => {
hamburger.classList.remove("open");
navMenu.classList.remove("open");
});
});
}
}
function setupTheme() {
const themeToggleBtn = document.getElementById("themeToggle");
const currentTheme = localStorage.getItem("theme");
if (currentTheme === "light") {
document.body.classList.add("light-mode");
}
if (themeToggleBtn) {
themeToggleBtn.addEventListener("click", () => {
document.body.classList.toggle("light-mode");
const theme = document.body.classList.contains("light-mode") ? "light" : "dark";
localStorage.setItem("theme", theme);
});
}
}
function setupLanguage() {
const langSelect = document.getElementById("langSelect");
let currentLang = localStorage.getItem("language") || "tr";
if (langSelect) {
langSelect.value = currentLang;
langSelect.addEventListener("change", (e) => {
const selectedLang = e.target.value;
localStorage.setItem("language", selectedLang);
updatePageLanguage(selectedLang);
document.documentElement.lang = selectedLang;
updateSelectedSessionDetails(selectedLang);
});
}
updatePageLanguage(currentLang);
}
function updatePageLanguage(lang) {
const translatableElements = document.querySelectorAll("[data-i18n]");
translatableElements.forEach(element => {
const key = element.getAttribute("data-i18n");
if (translations[key] && translations[key][lang]) {
element.textContent = translations[key][lang];
}
});
const nameInput = document.getElementById("regName");
if (nameInput) {
nameInput.placeholder = lang === "tr" ? "Örn: Ahmet Yılmaz" : "e.g. John Doe";
}
const idInput = document.getElementById("regID");
if (idInput) {
idInput.placeholder = lang === "tr" ? "11 Haneli T.C. Kimlik No" : "ID or Passport Number";
}
const emailInput = document.getElementById("regMail");
if (emailInput) {
emailInput.placeholder = lang === "tr" ? "ahmet@example.com" : "john@example.com";
}
const phoneInput = document.getElementById("regPhone");
if (phoneInput) {
phoneInput.placeholder = lang === "tr" ? "+90 (555) 123 4567" : "+1 (555) 123 4567";
}
const urlParams = new URLSearchParams(window.location.search);
const isIha3PreReg = urlParams.get("course") === "iha3" || urlParams.get("preRegisterCourse") === "iha3";
const addressInput = document.getElementById("regAddress");
const addressLabel = document.querySelector("label[for='regAddress']");
if (addressInput && addressLabel) {
if (isIha3PreReg) {
addressInput.placeholder = lang === "tr" ? "Paylaşmak istediğiniz ek detaylar, sorular veya notlar..." : "Additional details, questions, or notes you wish to share...";
addressLabel.textContent = lang === "tr" ? "Eklemek İstediğiniz Diğer Bilgiler (İsteğe Bağlı)" : "Additional Information You Wish to Share (Optional)";
} else {
addressInput.placeholder = lang === "tr" ? "Fatura ve resmi evrak adresi..." : "Billing & mailing address...";
addressLabel.textContent = lang === "tr" ? "Açık Adresiniz *" : "Open Address *";
}
}
const capacityOtherInput = document.getElementById("regCapacityOther");
if (capacityOtherInput) {
capacityOtherInput.placeholder = lang === "tr" ? "Örn: Dernek, Vakıf, Kamu Kurumu..." : "e.g. Association, Foundation, Public Institution...";
}
const docFile = document.getElementById("regDoc");
const docPreviewText = document.querySelector("#regDocPreview span");
if (docPreviewText && (!docFile || !docFile.files.length)) {
docPreviewText.textContent = translations["reg_select_file"][lang];
}
const photoFile = document.getElementById("regHeadshot");
const photoPreviewText = document.querySelector("#regHeadshotPreview span");
if (photoPreviewText && (!photoFile || !photoFile.files.length)) {
photoPreviewText.textContent = translations["reg_select_photo"][lang];
}
const idFrontFile = document.getElementById("regIdFront");
const idFrontPreviewText = document.querySelector("#regIdFrontPreview span");
if (idFrontPreviewText && (!idFrontFile || !idFrontFile.files.length)) {
idFrontPreviewText.textContent = translations["reg_select_photo"][lang];
}
const idBackFile = document.getElementById("regIdBack");
const idBackPreviewText = document.querySelector("#regIdBackPreview span");
if (idBackPreviewText && (!idBackFile || !idBackFile.files.length)) {
idBackPreviewText.textContent = translations["reg_select_photo"][lang];
}
}
let selectedSession = null;
function setupRegistrationSessions() {
const sessionSelect = document.getElementById("sessionSelect");
if (!sessionSelect) return;
const lang = localStorage.getItem("language") || "tr";
const urlParams = new URLSearchParams(window.location.search);
const isIha3PreReg = urlParams.get("course") === "iha3" || urlParams.get("preRegisterCourse") === "iha3";
if (isIha3PreReg) {
const wrapper = document.getElementById("sessionDropdownWrapper");
if (wrapper) wrapper.style.display = "none";
const payBox = document.querySelector(".payment-info-box");
if (payBox) payBox.style.display = "none";
const capWrapper = document.getElementById("regCapacityWrapper");
if (capWrapper) capWrapper.style.display = "block";
const capSelect = document.getElementById("regCapacity");
if (capSelect) capSelect.required = true;
const capOtherWrapper = document.getElementById("regCapacityOtherWrapper");
const capOtherInput = document.getElementById("regCapacityOther");
if (capSelect) {
capSelect.addEventListener("change", (e) => {
if (e.target.value === "other") {
if (capOtherWrapper) capOtherWrapper.style.display = "block";
if (capOtherInput) capOtherInput.required = true;
} else {
if (capOtherWrapper) capOtherWrapper.style.display = "none";
if (capOtherInput) {
capOtherInput.required = false;
capOtherInput.value = "";
}
}
});
}
const regIdInput = document.getElementById("regID");
if (regIdInput) {
regIdInput.required = false;
const group = regIdInput.closest(".form-group");
if (group) group.style.display = "none";
}
const fileIds = ["regDoc", "regHeadshot", "regIdFront", "regIdBack"];
fileIds.forEach(id => {
const el = document.getElementById(id);
if (el) {
el.required = false;
const group = el.closest(".form-group");
if (group) group.style.display = "none";
}
});
const regAddressInput = document.getElementById("regAddress");
if (regAddressInput) regAddressInput.required = false;
selectedSession = {
id: 999,
dateTextTR: "İHA-3 Eğitimi Ön Kayıt Başvurusu",
dateTextEN: "UAV-3 Pre-Registration Application",
typeTR: "Ön Kayıt",
typeEN: "Pre-Registration",
price: lang === "tr" ? "Açıklanacak" : "TBA",
timingTR: "Eğitim açıldığında bildirilecektir.",
timingEN: "To be announced when class opens.",
locationTR: "Sapmaz Academy Uçuş Sahaları",
locationEN: "Sapmaz Academy Flight Fields",
paymentTR: "Ön kayıt için ödeme talep edilmemektedir.",
paymentEN: "No payment is required for pre-registration."
};
} else {
sessionsData.forEach(session => {
const dates = lang === "tr" ? session.dateTextTR : session.dateTextEN;
const type = lang === "tr" ? session.typeTR : session.typeEN;
const option = document.createElement("option");
option.value = session.id;
option.textContent = `${dates} (${type})`;
sessionSelect.appendChild(option);
});
const paramSessionId = parseInt(urlParams.get("sessionId"));
if (paramSessionId) {
const found = sessionsData.find(s => s.id === paramSessionId);
if (found) {
sessionSelect.value = paramSessionId;
selectedSession = found;
}
}
if (!selectedSession && sessionsData.length > 0) {
selectedSession = sessionsData[0];
sessionSelect.value = selectedSession.id;
}
sessionSelect.addEventListener("change", (e) => {
const selectedId = parseInt(e.target.value);
const found = sessionsData.find(s => s.id === selectedId);
if (found) {
selectedSession = found;
updateSelectedSessionDetails(localStorage.getItem("language") || "tr");
}
});
}
updateSelectedSessionDetails(lang);
}
function updateSelectedSessionDetails(lang) {
const title = document.getElementById("selectedSessionTitle");
const price = document.getElementById("selectedSessionPrice");
const container = document.getElementById("selectedSessionDetails");
if (!selectedSession || !container) return;
const urlParams = new URLSearchParams(window.location.search);
const isIha3PreReg = urlParams.get("course") === "iha3" || urlParams.get("preRegisterCourse") === "iha3";
if (isIha3PreReg) {
selectedSession = {
id: 999,
dateTextTR: "İHA-3 Eğitimi Ön Kayıt Başvurusu",
dateTextEN: "UAV-3 Pre-Registration Application",
typeTR: "Ön Kayıt",
typeEN: "Pre-Registration",
price: lang === "tr" ? "Açıklanacak" : "TBA",
timingTR: "Eğitim açıldığında bildirilecektir.",
timingEN: "To be announced when class opens.",
locationTR: "Sapmaz Academy Uçuş Sahaları",
locationEN: "Sapmaz Academy Flight Fields",
paymentTR: "Ön kayıt için ödeme talep edilmemektedir.",
paymentEN: "No payment is required for pre-registration."
};
}
if (title) {
title.textContent = lang === "tr" ? selectedSession.dateTextTR : selectedSession.dateTextEN;
}
if (price) {
price.textContent = selectedSession.price;
}
const classType = lang === "tr" ? selectedSession.typeTR : selectedSession.typeEN;
const timingLabel = lang === "tr" ? "Zaman Bloğu" : "Time Block";
const timingValue = lang === "tr" ? selectedSession.timingTR : selectedSession.timingEN;
const locationLabel = lang === "tr" ? "Eğitim Konumu" : "Training Venue";
const locationValue = lang === "tr" ? selectedSession.locationTR : selectedSession.locationEN;
const paymentLabel = lang === "tr" ? "IBAN Adresi" : "Payment IBAN";
const paymentValue = lang === "tr" ? selectedSession.paymentTR : selectedSession.paymentEN;
container.innerHTML = `
<div class="detail-item">
<span class="detail-lbl">${lang === "tr" ? "Sınıf Türü" : "Class Type"}</span>
<span class="detail-val" style="color: var(--accent); font-weight: bold;">${classType}</span>
</div>
<div class="detail-item">
<span class="detail-lbl">${timingLabel}</span>
<span class="detail-val">${timingValue}</span>
</div>
<div class="detail-item">
<span class="detail-lbl">${locationLabel}</span>
<span class="detail-val">${locationValue}</span>
</div>
<div class="detail-item">
<span class="detail-lbl">${paymentLabel}</span>
<span class="detail-val" style="font-family: monospace; font-size: 0.8rem; letter-spacing: 0.5px;">${paymentValue}</span>
</div>
`;
}
function setupFilePreviews() {
const docFile = document.getElementById("regDoc");
const docPreviewText = document.querySelector("#regDocPreview span");
if (docFile && docPreviewText) {
docFile.addEventListener("change", (e) => {
if (e.target.files.length > 0) {
docPreviewText.textContent = e.target.files[0].name;
}
});
}
const photoFile = document.getElementById("regHeadshot");
const photoPreviewText = document.querySelector("#regHeadshotPreview span");
if (photoFile && photoPreviewText) {
photoFile.addEventListener("change", (e) => {
if (e.target.files.length > 0) {
photoPreviewText.textContent = e.target.files[0].name;
}
});
}
const idFrontFile = document.getElementById("regIdFront");
const idFrontPreviewText = document.querySelector("#regIdFrontPreview span");
if (idFrontFile && idFrontPreviewText) {
idFrontFile.addEventListener("change", (e) => {
if (e.target.files.length > 0) {
idFrontPreviewText.textContent = e.target.files[0].name;
}
});
}
const idBackFile = document.getElementById("regIdBack");
const idBackPreviewText = document.querySelector("#regIdBackPreview span");
if (idBackFile && idBackPreviewText) {
idBackFile.addEventListener("change", (e) => {
if (e.target.files.length > 0) {
idBackPreviewText.textContent = e.target.files[0].name;
}
});
}
}
function setupSubmitHandler() {
const regForm = document.getElementById("courseRegistrationForm");
const successMessage = document.getElementById("regSuccessMessage");
const successBackBtn = document.getElementById("regSuccessBackBtn");
if (!regForm || !successMessage) return;
regForm.addEventListener("submit", (e) => {
e.preventDefault();
const lang = localStorage.getItem("language") || "tr";
const name = document.getElementById("regName").value;
const idNumber = document.getElementById("regID").value;
const email = document.getElementById("regMail").value;
const phone = document.getElementById("regPhone").value;
const docFile = document.getElementById("regDoc").files[0];
const photoFile = document.getElementById("regHeadshot").files[0];
const idFrontFile = document.getElementById("regIdFront").files[0];
const idBackFile = document.getElementById("regIdBack").files[0];
const openAddress = document.getElementById("regAddress").value;
const docName = docFile ? docFile.name : "-";
const photoName = photoFile ? photoFile.name : "-";
const idFrontName = idFrontFile ? idFrontFile.name : "-";
const idBackName = idBackFile ? idBackFile.name : "-";
const urlParams = new URLSearchParams(window.location.search);
const isIha3PreReg = urlParams.get("course") === "iha3" || urlParams.get("preRegisterCourse") === "iha3";
const sessionDates = lang === "tr" ? selectedSession.dateTextTR : selectedSession.dateTextEN;
const sessionClass = lang === "tr" ? selectedSession.typeTR : selectedSession.typeEN;
const fileTitle = isIha3PreReg
? (lang === "tr" ? "SAPMAZ ACADEMY - İHA-3 ÖN KAYIT BAŞVURU FORMU" : "SAPMAZ ACADEMY - UAV-3 PRE-REGISTRATION APPLICATION")
: (lang === "tr" ? "SAPMAZ ACADEMY - ONLINE KURS KAYIT FORMUMUZ" : "SAPMAZ ACADEMY - ONLINE COURSE REGISTRATION");
const fileSessionLabel = lang === "tr" ? "Dönem / Sınıf" : "Session / Class";
const filePriceLabel = isIha3PreReg 
? (lang === "tr" ? "Eğitim Ücreti" : "Course Fee") 
: (lang === "tr" ? "Dönem Ücreti" : "Session Fee");
const fileIbanLabel = isIha3PreReg
? (lang === "tr" ? "Ödeme Bilgisi" : "Payment Info")
: (lang === "tr" ? "IBAN Adresi" : "Payment IPAN");
const fileNameLabel = lang === "tr" ? "Ad Soyad" : "Full Name";
const fileCapacityLabel = lang === "tr" ? "Başvuru Kapasitesi" : "Application Capacity";
const fileIdLabel = lang === "tr" ? "T.C. Kimlik / Pasaport" : "ID or Passport No";
const fileEmailLabel = lang === "tr" ? "E-Posta Adresi" : "Email Address";
const filePhoneLabel = lang === "tr" ? "Telefon No" : "Phone Number";
const fileDocLabel = lang === "tr" ? "Belge Dosyası" : "Student/Grad File";
const filePhotoLabel = lang === "tr" ? "Vesikalık Dosyası" : "Headshot File";
const fileIdFrontLabel = lang === "tr" ? "Kimlik Ön Yüzü" : "Front of ID File";
const fileIdBackLabel = lang === "tr" ? "Kimlik Arka Yüzü" : "Back of ID File";
const fileAddressLabel = isIha3PreReg
? (lang === "tr" ? "Ek Bilgiler" : "Additional Info")
: (lang === "tr" ? "Açık Adres" : "Open Address");
const fileDateLabel = lang === "tr" ? "Başvuru Tarihi" : "Application Date";
const timestamp = new Date().toLocaleString(lang === "tr" ? "tr-TR" : "en-US");
const capacityEl = document.getElementById("regCapacity");
let capacityValueText = "";
if (capacityEl && isIha3PreReg) {
const selectedOpt = capacityEl.options[capacityEl.selectedIndex];
if (selectedOpt && selectedOpt.value === "other") {
const specifyEl = document.getElementById("regCapacityOther");
const specifyVal = specifyEl ? specifyEl.value.trim() : "";
capacityValueText = selectedOpt.text + (specifyVal ? ` (${specifyVal})` : "");
} else {
capacityValueText = selectedOpt ? selectedOpt.text : "-";
}
}
const registrationReportText = isIha3PreReg ? `==================================================
${fileTitle}
==================================================
${fileDateLabel.padEnd(20)} : ${timestamp}
--------------------------------------------------
${fileSessionLabel.padEnd(20)} : ${sessionDates} (${sessionClass})
${filePriceLabel.padEnd(20)} : ${selectedSession.price}
${fileIbanLabel.padEnd(20)} : ${selectedSession.paymentTR}
--------------------------------------------------
${fileNameLabel.padEnd(20)} : ${name}
${fileCapacityLabel.padEnd(20)} : ${capacityValueText}
${fileEmailLabel.padEnd(20)} : ${email}
${filePhoneLabel.padEnd(20)} : ${phone}
--------------------------------------------------
${fileAddressLabel}:
--------------------------------------------------
${openAddress || "-"}
==================================================
` : `==================================================
${fileTitle}
==================================================
${fileDateLabel.padEnd(20)} : ${timestamp}
--------------------------------------------------
${fileSessionLabel.padEnd(20)} : ${sessionDates} (${sessionClass})
${filePriceLabel.padEnd(20)} : ${selectedSession.price}
${fileIbanLabel.padEnd(20)} : ${selectedSession.paymentTR}
--------------------------------------------------
${fileNameLabel.padEnd(20)} : ${name}
${fileIdLabel.padEnd(20)} : ${idNumber}
${fileEmailLabel.padEnd(20)} : ${email}
${filePhoneLabel.padEnd(20)} : ${phone}
--------------------------------------------------
${fileDocLabel.padEnd(20)} : ${docName}
${filePhotoLabel.padEnd(20)} : ${photoName}
${fileIdFrontLabel.padEnd(20)} : ${idFrontName}
${fileIdBackLabel.padEnd(20)} : ${idBackName}
--------------------------------------------------
${fileAddressLabel}:
--------------------------------------------------
${openAddress}
==================================================
`;
try {
const blob = new Blob([registrationReportText], { type: "text/plain;charset=utf-8" });
const link = document.createElement("a");
link.href = URL.createObjectURL(blob);
const cleanName = name.trim().toLowerCase().replace(/[^a-z0-9]/g, "_");
link.download = `sapmaz_registration_${cleanName || "request"}.txt`;
document.body.appendChild(link);
link.click();
document.body.removeChild(link);
URL.revokeObjectURL(link.href);
} catch (err) {
console.error("Failed to download registration text file:", err);
}
regForm.style.display = "none";
successMessage.style.display = "block";
const mainContainer = document.querySelector(".news-page");
if (mainContainer) {
mainContainer.scrollIntoView({ behavior: "smooth" });
}
});
if (successBackBtn) {
successBackBtn.addEventListener("click", () => {
window.location.href = "../index.html";
});
}
}